#include <stdio.h>

int main() {
    int num;

    printf("Enter an integer: ");
    scanf("%d", &num);

    if (num % 7 == 0 || num % 10 == 7)
        printf("It is a Buzz Number.");
    else
        printf("It is NOT a Buzz Number.");

    return 0;
}
